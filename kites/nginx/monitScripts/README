add following lines to  /etc/monit.d/nginxApi:

check process nginxApi
    with pidfile /var/run/nginxApi.pid
    start program = "/opt/kfmjs/kites/nginx/monitScripts/nginxApi.sh start"
        with timeout 30 seconds
    stop program = "/opt/kfmjs/kites/nginx/monitScripts/nginxApi.sh stop"
        with timeout 30 seconds

    if changed pid then alert
    if totalcpu > 5% for 2 cycles then alert
    if totalmem > 30 MB for 2 cycles then alert
    if 3 restarts within 5 cycles then timeout