#!/bin/bash

SCRIPTS=$(dirname $0)/../scripts

HOST=$(cat INSTANCE_DATA | awk '{print $2}')

if [ -z "$HOST" ]; then exit 1; fi

$SCRIPTS/test-instance/check-connectivity $HOST 8090 1m 20
$SCRIPTS/test-instance/check-connectivity $HOST 4444 1m 20
