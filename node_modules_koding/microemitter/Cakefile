fs = require 'fs'
coffee = require 'coffee-script'

getJs =->
  coffee.compile fs.readFileSync('./src/eventemitter.coffee', 'utf-8'), bare:yes

task 'build', 'Build the emitter for the browser and Node', (options)->
  js = getJs()
  fs.writeFileSync 'index.js', js, 'utf8'