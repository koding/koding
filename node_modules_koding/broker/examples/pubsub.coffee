Broker = require "broker"

# options = {
#     host: "web-prod.in.koding.com",
#     login: "guest",
#     password: "x1srTA7!%Vb}$n|S"
#   }
options = {host: "localhost"}
channel = "test_channel"
event = "test_event"
message = "Hello"

broker = new Broker options, ->
    console.log "Ready"
    broker.on channel, event, (msg) ->
        console.log msg+""

    setTimeout ->
        broker.trigger channel, event, message
    , 500

    #setTimeout (-> broker.emit channel, event, message), 100
    #process.exit(code=0)
    
