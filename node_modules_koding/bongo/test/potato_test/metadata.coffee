Model = require '../../lib/model'

module.exports = class Metadata extends Model

  @setSchema
    createdAt :
      type    : Date
      default : -> new Date
    modifiedAt:
      type    : Date
      default : -> new Date
    views     : Number
    likes     : Number
    dislikes  : Number
    tags      : [String]
