{ dash } = require '../../lib'
{ @dash } = require 'bongo'
queue = []
for i in [0..100]
  do (i) ->
    queue.push ->
      console.log 'begin', i
      setTimeout ->
        console.log 'fin', i
        queue.fin()
      , Math.random() * 1000
@dash queue, ->
  console.log 'FIN!'
