{ Model } = require 'bongo'

class @Note extends Model

  @share()
  @set
    sharedMethods :
      instance    : ['save', 'update', 'on']
    schema        :
      subject     :
        type      : String
        required  : yes
      body        : String
