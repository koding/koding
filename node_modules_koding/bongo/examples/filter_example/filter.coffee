{ Model } = require '../../lib'


class FilterExample extends Model

  @setSchema
    prop      :
      type    : String
      filter  : (raw) -> raw + '123'

example = new FilterExample { prop: 'whoohoo!' }

console.log example.prune()
