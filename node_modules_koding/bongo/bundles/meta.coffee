Model = require '../lib/model'

module.exports = class Meta extends Model

  @setSchema
    createdAt :
      type    : Date
      default : -> new Date
    modifiedAt:
      type    : Date
      default : -> new Date
    tags      : Object
    views     : Number
    votes     :
      up      : Number
      down    : Number
    likes     :
      type    : Number
      default : 0
