Model = require '../lib/model'

module.exports = class Meta extends Model

  @setSchema
    createdAt :
      type    : Date
      default : -> new Date
    modifiedAt:
      type    : Date
      default : -> new Date
