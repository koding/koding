# kite worker example
{Worker} = require 'koding-kite'

class MathWorker extends Worker
  @kiteName = 'mathworker'
  @username = 'devrim'
  square: (request, callback)->
    console.log "square called !", arguments
    callback null, request.args * request.args

mathworker = new MathWorker
mathworker.runServer()
