# kite worker example
{Worker} = require 'koding-kite'

worker = new Worker name: "mathworker", username: "devrim"
worker.handleFunc "square", (request, callback)->
  request.remoteKite.callRemote "log", "received args"
  callback null, request.args * request.args
worker.runServer()
