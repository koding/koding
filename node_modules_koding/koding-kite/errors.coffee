class KiteError extends Error
  constructor: (options)->
    if typeof options is "string"
        @type = "Generic"
        @message = options
    else
        @type    = options.type or "Generic"
        @message = options.message or ""

module.exports.KiteError = KiteError