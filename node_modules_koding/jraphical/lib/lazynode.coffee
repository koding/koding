{ Model, Base } = require 'bongo'

module.exports = class LazyNode
  constructor:(@typeName, @_id) ->
    @type = Base.constructors[typeName]
    @lazyNode = yes

  load:(callback) -> @type.one { @_id }, callback
