DomainService = require './DomainService.coffee'
CustomerService = require './CustomerService.coffee'
DNSManager = require './DNSManager.coffee'

module.exports = class DomainManager

  constructor:->
    @customerService = new CustomerService 
    @domainService = new DomainService
    @dnsManager = new DNSManager
