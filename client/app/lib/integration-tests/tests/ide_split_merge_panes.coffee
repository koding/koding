$ = require 'jquery'
assert = require 'assert'

#! fcf4e1e6-c95c-41b8-8335-5949d44256c6
# title: ide_split_merge_panes
# start_uri: /
# tags: automated
#

describe "ide_split_merge_panes.rfml", ->
  before -> 
    require './create_team_with_existing_user_stack_related.coffee'

  describe """Click on the close button (x) on the pane that you want to close""", ->
    before -> 
      # implement before hook 

    it """Is the tabs merged ??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Click on the (+) button from top left corner of the bottom pane and select 'Split Vertically' option""", ->
    before -> 
      # implement before hook 

    it """Is that pane splitted to the right and left sections??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Click on the close button (x) on the pane that you want to close""", ->
    before -> 
      # implement before hook 

    it """Is the tabs merged ??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Double click '.profile' file in the file tree""", ->
    before -> 
      # implement before hook 

    it """Do you see new editor tab opened with this file content??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Click and hold the file tab titled '.profile' and drag it to bottom half of editor area and hold it for a couple of seconds and release""", ->
    before -> 
      # implement before hook 

    it """Did you see the 'Drop to move source pane to this split' message in green backfround?""", -> 
      assert(false, 'Not Implemented')
      #assertion here

    it """Did pane split in half after you released??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Click on the (x) icon appearing when you mouse over the top right corner or the 'bottom' pane until there's nothing left on the bottom pane""", ->
    before -> 
      # implement before hook 

    it """Is bottom pane removed?""", -> 
      assert(false, 'Not Implemented')
      #assertion here

    it """Is there only one pane left??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Mouse over the top right corner of the remaining pane""", ->
    before -> 
      # implement before hook 

    it """Is (x) icon not visible there?""", -> 
      assert(false, 'Not Implemented')
      #assertion here

    it """(It shouldn't be visible when there's only one pane left)?""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Click settings icon at the top of the filetree""", ->
    before -> 
      # implement before hook 

    it """Do you see 'Editor Settings' instead of filetree??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Click file tree icon next to settings icon at the top of the filetree""", ->
    before -> 
      # implement before hook 

    it """Do you see file tree again??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Save image from here: {{ random.image }} to your desktop, return to the first browser and drag and drop image to the Filetree under '/home/rainforestqa99' label (like in the screencast: http://recordit.co/KRArrgJ8Gs)""", ->
    before -> 
      # implement before hook 

    it """Is it added to the end of Filetree??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Double click '.profile' file in the file tree""", ->
    before -> 
      # implement before hook 

    it """Do you see new editor tab opened with this file content??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """Enter 'se' in the last row in '.profile' file""", ->
    before -> 
      # implement before hook 

    it """Are suggestions for autocomplete displayed as a dropdown??""", -> 
      assert(false, 'Not Implemented')
      #assertion here


  describe """""", ->
    before -> 
      # implement before hook 

  describe """""", ->
    before -> 
      # implement before hook 

