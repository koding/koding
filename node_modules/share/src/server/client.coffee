# This is the API that the sharejs server frontends use to represent actions by connected
# clients.

# The file exposes a function which creates clients.
#
# model is the 
model.exports = (model, auth, data) ->


